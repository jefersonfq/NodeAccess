{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nodeaccess.serviceAccountName" . }}
  labels:
    {{- include "nodeaccess.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
